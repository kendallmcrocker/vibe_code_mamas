# Secrets — never commit
.env
.env.local
*.key

# OS / editor
.DS_Store

# Local Claude Code settings (machine-specific)
.claude/settings.local.json

# Dependencies / build (add more as the stack takes shape)
node_modules/
__pycache__/
*.pyc
.venv/
venv/
dist/
build/
